name: Keep Alive
on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  Ping:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: Curl URL
        run: |
          curl https://proxy.jupitersleo.repl.co/
